services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    image: frontend:latest
    container_name: frontend
    ports:
      - "80:80"
    restart: unless-stopped
